// Infinity loop - must be terminated manually using ctrl(cmd) + c
// let count = 0;

// while (true) {
// 	console.log(`Loop running ${count++}`);
// }

// natural terminating loop
let count = 0;

const studentEmails = ['john@student'];

while (count < 5) {
	console.log(`Count ${count}`);
	// terminating condition
	// count = count + 2;
}

console.log(count);
